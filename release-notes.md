0.4.9 本机凭据空间 · 每次操作确认

【本机凭据空间】
· 管理台新增凭据空间，可保存和恢复原值；由当前 Windows 用户的系统机制加密，无需另设一把密钥
· 保存、查看、删除每次都弹出原生确认；查看原值只在窗口中短暂显示 15 秒
· 凭据不进入记忆检索、蒸馏、同步或普通导出；Agent 的 MCP 工具不能读取原值
· 按全局、项目、个人作用域整理；项目作用域使用仓库名

【兼容说明】
· 从 0.4.8 升级即可；数据库自动升到 schema 7，旧记忆与密钥不变
· 凭据由保存时的 Windows 用户保护；复制数据库到另一台机器不能直接解锁原值
· 仅有 MCP 权限的 Agent 无法读取凭据；若 Agent 拥有同一 Windows 用户的本机执行权限，需用系统权限另行隔离