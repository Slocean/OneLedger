0.4.8 记忆写入可信 · 采集去重 · 蒸馏可跟踪

【记忆写入】
· remember 明确返回 stored / queued / rejected / unchanged / conflict / error，不再靠 redacted 猜结果
· 蒸馏覆盖接受 expectedRev：版本对不上就返回冲突并保留原文，不会静默覆盖别人的记忆
· 管理台「确认所选来源」改成一笔事务：校验来源、写正式记忆、删来源同时成功或同时不动
· 入正式库前对脱敏后的正文再扫一遍，仍有残留凭据直接拒收，并给出规则与行号

【采集】
· 新增采集指纹：同一文件内容没变就不再重复入队，重复扫描不再堆队列
· 被拒收的材料也记指纹，不会每半小时重复产生拒收记录

【读取】
· search / get / list 不再顺手清理旧记忆；读操作只读，整理交给管理台显式维护

【蒸馏队列】
· 新增「待蒸馏任务」视图：按仓库聚合，显示待处理条数、最旧等待时长、高信号来源
· 可接模型生成草稿（管理员侧），草稿只待审、不自动入账；来源或版本变化会标记过期
· 没有配模型也能完整手工整理，路径不变

【检索】
· 补了中文短语、英文关键词、跨作用域评测；本机 5000 篇规模查询在毫秒级

【兼容说明】
· 从 0.4.7 升级即可；数据库自动升到 schema 6（新增指纹表与草稿表），旧数据不动
· 账本仍是 global / project / personal + 同作用域覆盖；数据仍在 ~/.oneledger
· Agent 仍只有 search / remember / forget / list / get 五个工具，未开放 inbox 或 secret